const form=document.getElementById('contact-form');let id=crypto.randomUUID();form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button');const feedback=document.getElementById('contact-feedback');button.disabled=true;button.textContent='SAVING…';const data=Object.fromEntries(new FormData(form));try{const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,id})});const result=await response.json();if(!response.ok)throw new Error(result.error);feedback.textContent='Your message is saved for the Frilix team to review. Thank you.';form.reset();id=crypto.randomUUID();}catch(error){feedback.textContent=error.message||'Unable to save. Please retry.';}finally{button.disabled=false;button.textContent='SAVE MESSAGE';}});
